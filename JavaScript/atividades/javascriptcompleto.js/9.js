//Elabore uma função que recebe um número como parâmetro e retorne uma string com o símbolo "+" na quantidade especificada no parâmetro.

function returne (number){
var valor = []
    for (i=0; i < number; i++){
        valor[i] = "+" 
    }
    return console.log(valor);
}
returne(5)